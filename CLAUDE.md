# TICO — Claude Code Guide

The authoritative agent rules for this repository live in `AGENTS.md` and are shared
with other coding agents (e.g. Codex). Do not duplicate rules here; edit `AGENTS.md`
instead so all agents stay in sync.

@AGENTS.md

Subdirectory rules (`test/`, `tico/passes/`, `tico/quantization/`) are loaded
automatically from the `CLAUDE.md` in each directory when working on files there.
